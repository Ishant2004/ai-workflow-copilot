# Production config TEMPLATE. Copy to .env.production (git-ignored) or inject at
# build/deploy time. NEXT_PUBLIC_* is baked into the client bundle at build.
NEXT_PUBLIC_API_BASE_URL=https://api.example.com
