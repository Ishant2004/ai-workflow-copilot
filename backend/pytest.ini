[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
markers =
    unit: fast, isolated tests with no external dependencies
    integration: tests that require running infra (Postgres, Redis)
